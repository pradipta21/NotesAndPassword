server:
  port: 9001

eureka:
  instance:
    hostname: localhost

spring:
  application:
    name: note-service
  datasource:
    url: jdbc:postgresql://localhost:5432/nap
    username: postgres
    password: password@12
    pool-size: 30
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true


